@host = localhost
@port = 7071
@baseUrl = http://{{host}}:{{port}}

### Simple GET search
GET {{baseUrl}}/api/websearch?q=searxng
Accept: application/json

### GET with multiple engines and options
GET {{baseUrl}}/api/websearch?q=privacy+search&engines=duckduckgo,brave&language=en&pageno=1&time_range=year&safesearch=0&max_results=5
Accept: application/json

### GET (French) with a single engine
GET {{baseUrl}}/api/websearch?q=moteur+de+recherche&engines=brave&language=fr&max_results=3
Accept: application/json

### POST search (recommended for complex payloads)
POST {{baseUrl}}/api/websearch
Content-Type: application/json
Accept: application/json

{
  "query": "searxng self hosted",
  "engines": ["brave"],
  "language": "en",
  "pageno": 1,
  "time_range": "month",
  "safesearch": 0,
  "max_results": 5
}

### POST search (French, multiple engines)
POST {{baseUrl}}/api/websearch
Content-Type: application/json
Accept: application/json

{
  "query": "confidentialité web",
  "engines": ["duckduckgo", "brave"],
  "language": "fr",
  "max_results": 5
}

### Note:
# If your function runs on a different port (e.g. 7081), change @port above.

